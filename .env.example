# Optional: use only for local development with a local TableScanner instance.
# When set, the viewer uses this URL instead of config/index.json for the TableScanner API.
# Leave unset (or do not create .env) when testing deployment parity or when building for deployment.
#
# VITE_API_URL=http://127.0.0.1:8000
